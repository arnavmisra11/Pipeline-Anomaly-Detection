# Pipeline-Anomaly-Detection
This project uses Streamlit to provide a web-based anomaly detection tool enabling interactive analysis of numerical CSV data. Users upload datasets, select columns, and visualize trends with line charts. Anomalies are detected using mean and standard deviation, flagging values outside mean ± 2 standard deviations as abnormal for basic analysis.
